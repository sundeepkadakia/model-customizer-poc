FROM python:3.12-slim
WORKDIR /app
COPY requirements-cloud.txt .
RUN pip install --no-cache-dir -r requirements-cloud.txt
COPY app ./app
ENV PYTHONUNBUFFERED=1
CMD exec uvicorn app.main_cloud:app --host 0.0.0.0 --port ${PORT:-8080}
